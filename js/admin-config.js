/**
 * Código de acceso del panel privado (admin.html).
 *
 * IMPORTANTE — leer antes de usar el panel:
 * Esto NO es seguridad real. Este sitio es 100% estático (no tiene
 * servidor propio), así que cualquier persona que sepa inspeccionar el
 * código de la página (clic derecho → "Ver código fuente") puede leer este
 * valor tal cual está escrito aquí. Este código solo sirve como un filtro
 * básico para que alguien no entre por accidente si llega a encontrar el
 * link del panel — no protege datos sensibles de alguien que de verdad
 * quiera entrar.
 *
 * Recomendaciones:
 * - No compartas el link de admin.html públicamente (no lo pongas en
 *   Instagram, WhatsApp de difusión, etc.).
 * - No guardes en el panel información que no puedas permitirte que se
 *   filtre si alguien llegara a encontrar el link.
 * - Exporta a CSV regularmente y guarda una copia en tu computador
 *   (Google Drive, Excel local, etc.) — los datos solo viven en el
 *   navegador donde los ingresaste.
 * - Cambia este código cuando quieras, solo edita el texto de abajo.
 */
const ADMIN_CONFIG = {
  passcode: "altitude2024",
};
